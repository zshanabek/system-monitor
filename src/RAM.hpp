#ifndef RAM_HPP
# define RAM_HPP
# include <iostream>
# include <string>

class RAM
{
	public:
		RAM ();
		~RAM ();
		RAM (RAM const & copy);
		RAM & operator=(RAM const & copy);

	private:

};

#endif